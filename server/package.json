{
  "name": "military-asset-tracker-server",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "tsx index.ts",
    "build": "npm install",
    "build:client": "cd ../client && npm install && npm run build && mkdir -p ../server/public && cp -r ../dist/public/* ../server/public/",
    "dev": "tsx watch index.ts"
  },
  "dependencies": {
    "express": "^4.21.2",
    "typescript": "^5.0.0",
    "tsx": "^4.7.0"
  }
} 